[workspace]
members = ["act-cli", "act-build"]
resolver = "3"

[workspace.dependencies]
act-types = { version = "0.2.8", path = "../act-sdk-rs/act-types" }
wasmparser = "0.246"

[profile.release]
lto = "fat"
codegen-units = 1
strip = true
opt-level = 3
