name: CI

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always

jobs:
  check:
    name: Check & Clippy
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7
      - uses: moonrepo/setup-rust@v1
        with:
          components: clippy
          bins: wkg
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - run: wkg wit fetch --type wit
        working-directory: act-cli
        env:
          WKG_CONFIG_FILE: wkg-registry.toml
      - run: cargo clippy -- -D warnings

  fmt:
    name: Format
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v7
      - uses: moonrepo/setup-rust@v1
        with:
          components: rustfmt
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - run: cargo fmt -- --check

  test:
    name: Test
    needs: [check, fmt]
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7
      - uses: moonrepo/setup-rust@v1
        with:
          bins: wkg, cargo-nextest
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - run: wkg wit fetch --type wit
        working-directory: act-cli
        env:
          WKG_CONFIG_FILE: wkg-registry.toml
      - run: cargo nextest run

  build:
    needs: [check, fmt]
    uses: ./.github/workflows/build.yml

  build-pypi:
    needs: [check, fmt]
    uses: ./.github/workflows/build-pypi.yml

  build-npm:
    needs: build
    uses: ./.github/workflows/build-npm.yml

  build-docker:
    needs: build
    uses: ./.github/workflows/build-docker.yml
    with:
      push: false

  build-sbom:
    needs: [check, fmt]
    uses: ./.github/workflows/build-sbom.yml
